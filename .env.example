# OpenAI 兼容接口（OpenAI / DeepSeek / SiliconFlow 等均可）
# 本地：复制为 .env.local 并填入真实值（勿提交 Git）
# 线上：在 Vercel → Settings → Environment Variables 配置同名变量后 Redeploy
VITE_LLM_API_KEY=
VITE_LLM_API_BASE_URL=https://api.openai.com/v1
VITE_LLM_MODEL=gpt-4o-mini
